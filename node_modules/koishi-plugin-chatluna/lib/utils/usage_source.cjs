var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils/usage_source.ts
var usage_source_exports = {};
__export(usage_source_exports, {
  usageSourceFromStack: () => usageSourceFromStack
});
module.exports = __toCommonJS(usage_source_exports);
var import_fs = __toESM(require("fs"), 1);
var import_path = __toESM(require("path"), 1);
var import_url = require("url");
function usageSourceFromStack(stack) {
  if (!stack) return "unknown";
  let fallback;
  for (const line of stack.split("\n")) {
    const match = line.match(
      /^\s*at\s+(?:[^()]+\()?((?:[A-Za-z]:[\\/]|\/|file:\/\/\/).+):\d+:\d+\)?$/
    ) ?? line.match(/^((?:[A-Za-z]:[\\/]|\/|file:\/\/\/).+):\d+:\d+$/);
    if (!match) continue;
    const file = match[1].startsWith("file:///") ? (0, import_url.fileURLToPath)(match[1]) : import_path.default.resolve(match[1]);
    const slash = file.replaceAll("\\", "/");
    let dir = import_path.default.dirname(file);
    while (dir !== import_path.default.dirname(dir)) {
      const pkg = import_path.default.join(dir, "package.json");
      if (import_fs.default.existsSync(pkg)) {
        const name = JSON.parse(import_fs.default.readFileSync(pkg, "utf8")).name;
        if (typeof name === "string") {
          const base = name.split("/").pop()?.replaceAll("_", "-");
          if (!base?.startsWith("koishi-plugin-")) break;
          const source = base.slice("koishi-plugin-".length);
          if (source && !name.startsWith("@koishijs/") && !name.startsWith("@cordisjs/") && source !== "chatluna") {
            return source.endsWith("-entry-point") ? source.slice(0, -"-entry-point".length) : source;
          }
          if (source === "chatluna") {
            fallback = source;
          }
        }
        break;
      }
      dir = import_path.default.dirname(dir);
    }
    if (slash.includes("/packages/core/src/") || slash.includes("/packages/core/lib/") || slash.includes("/node_modules/koishi-plugin-chatluna/")) {
      continue;
    }
  }
  return fallback ?? "unknown";
}
__name(usageSourceFromStack, "usageSourceFromStack");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  usageSourceFromStack
});
