import type { Session } from 'koishi';
import type { SubagentContext } from '../types';
import type { ChatLunaToolRunnable } from '../../platform/types';
import type { ChatLunaAgent } from '../agent';
import type { ActiveAgentTaskRun, AgentTaskInput, AgentTaskRun, AgentTaskSession, AgentTaskSessionSnapshot, AgentTaskTarget, CreateTaskToolOptions } from './types';
export declare function runAgentTask(options: {
    input: AgentTaskInput;
    toolName: string;
    runtime: CreateTaskToolOptions;
    tasks: Map<string, AgentTaskSession>;
    runs: Map<string, AgentTaskRun>;
    active: Map<string, ActiveAgentTaskRun>;
    snapshots: Map<string, AgentTaskSessionSnapshot>;
    scheduleRunCleanup: (runId: string) => void;
    scheduleTaskCleanup: (taskId: string) => void;
    task: AgentTaskSession;
    target: AgentTaskTarget;
    prompt: string;
    session: Session;
    conversationId: string;
    source: 'chatluna' | 'character';
    parent?: SubagentContext;
    signal?: AbortSignal;
    runConfig?: ChatLunaToolRunnable;
}): Promise<string>;
export declare function createTaskSession(agent: ChatLunaAgent, parentConversationId: string, session: Session, parent?: SubagentContext, maxDepth?: number): AgentTaskSession;
